server.port = 3333

# ## Local db filestore: in base directory in folder "data"
#spring.datasource.url=jdbc:h2:file:./employees_db;DB_CLOSE_ON_EXIT=FALSE

# h2 defaults:
#spring.datasource.username=sa
#spring.datasource.password=

#spring.datasource.driverClassName=org.h2.Driver

# Use "create" or "create-drop" when you wish to recreate a database on restart; use "update" or "validate" when data is to be kept.
spring.jpa.hibernate.ddl-auto=create-drop

spring.jpa.properties.hibernate.allow_jvm_access=true
spring.jpa.properties.hibernate.bytecode.provider=javassist

spring.hateoas.use-hal-as-default-json-media-type=false
